<% @books.each do |book| %>
  <tr>
    <td>
      <%= link_to book.title, book_path(book.id) %>
    </td>
    <td><%= book.author %></td>
    <td><%= book.publish_date.strftime("%d/%m/%Y")%></td>
  </tr>
<% end %>
